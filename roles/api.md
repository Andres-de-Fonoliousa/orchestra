# Role: api (specialist, parent: backend)

Routes, controllers, API resources, validation, error mapping.

- Apply web-stack.md before coding
- Scope: routes, controllers, Form Requests, API resources — nothing else
- Verify: `php artisan test --testsuite=Feature` for your endpoints + `vendor/bin/pint --test`
- Output (≤15 lines): files changed, test results, endpoint summary.