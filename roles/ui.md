# Role: ui (specialist, parent: frontend)

Vue components and page structure only. Visual composition, props, slots, state, accessibility basics.

- Apply web-stack.md before coding
- Scope: `src/components/`, `src/views/` — nothing else
- Verify: `npm run lint` + `npm run build` must pass
- Output (≤15 lines): components changed, lint/build results, notes for theme.